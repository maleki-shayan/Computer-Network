#ifndef EXPERIMENT_HPP
#define EXPERIMENT_HPP

#include <string>

using namespace std;

const string TEST_NAME = "normal";
const int LOSS_PERCENT = 0;
const int ACK_DELAY_MS = 0;

const string PHASE5_DIRECTORY = "results/phase5/" + TEST_NAME;

#endif
